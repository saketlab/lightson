% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admin.R
\name{get_india_admin}
\alias{get_india_admin}
\title{Get Indian administrative boundaries}
\usage{
get_india_admin(
  level = c("country", "state", "district"),
  source = c("gadm", "bhuvan")
)
}
\arguments{
\item{level}{One of \code{"country"}, \code{"state"}, or \code{"district"}.}

\item{source}{One of \code{"gadm"} (default) or \code{"bhuvan"}. For \code{level = "state"},
the default uses a bundled LGD dataset with SoI-compliant boundaries
regardless of \code{source}. \code{"bhuvan"} fetches geometries from the Bhuvan API
so that names match \code{\link[=bhuvan_stats]{bhuvan_stats()}} output exactly.}
}
\value{
An \code{sf} object.
}
\description{
Returns Indian administrative boundaries as an \code{sf} object. Useful as the
\code{polygons} argument to \code{\link[=extract_panel]{extract_panel()}}.
}
\details{
State boundaries use a bundled LGD (Local Government Directory) dataset
derived from the Survey of India.

For custom boundaries, load your own file with \code{sf::read_sf()} and pass it
directly to \code{\link[=extract_panel]{extract_panel()}} -- no wrapper needed.
}
\examples{
\dontrun{
states <- get_india_admin("state")
districts <- get_india_admin("district")

# custom boundary:
districts <- sf::read_sf("https://bharatviz.org/India-bhuvan-districts.geojson")
}
}
