% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract_panel}
\alias{extract_panel}
\title{Compute zonal statistics for a panel of rasters}
\usage{
extract_panel(rasters, polygons, fun = "mean", id_col = NULL)
}
\arguments{
\item{rasters}{A named list of \code{SpatRaster} objects. Names should be years
(e.g., \code{"2020"}, \code{"2021"}). Returned by \code{\link[=ntl_download]{ntl_download()}} or
\code{\link[=bhuvan_raster]{bhuvan_raster()}}.}

\item{polygons}{An \code{sf} object or \code{SpatVector} containing the regions to
summarise over.}

\item{fun}{Aggregation function name passed to \code{\link[terra:zonal]{terra::zonal()}}. Default
\code{"mean"}. Also accepts \code{"sum"}, \code{"median"}, etc.}

\item{id_col}{Name of the column in \code{polygons} to use as \code{region_id}. If
\code{NULL} (default), the first character column is used.}
}
\value{
A data frame with columns:
\describe{
\item{region_id}{Character. Region identifier from \code{polygons}.}
\item{year}{Integer. Year, taken from the name of each raster.}
\item{mean_radiance}{Numeric. Aggregated radiance value.}
\item{n_pixels}{Integer. Number of non-NA cells contributing to the stat.}
}
}
\description{
Takes a named list of \code{SpatRaster} objects (one per year, as returned by
\code{\link[=ntl_download]{ntl_download()}} or \code{\link[=bhuvan_raster]{bhuvan_raster()}}) and an \code{sf} or \code{SpatVector} polygon
layer, and returns a tidy long-format data frame with one row per
region per year.
}
\details{
The \code{polygons} argument accepts any \code{sf} object -- GADM, bharatviz GeoJSONs,
LGD districts, or any shapefile loaded with \code{sf::read_sf()}.
}
\examples{
\dontrun{
districts <- sf::read_sf("https://bharatviz.org/India-bhuvan-districts.geojson")
rasters <- bhuvan_raster(districts, years = 2018:2023)
panel <- extract_panel(rasters, districts)
head(panel)
}
}
