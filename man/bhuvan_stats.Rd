% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_bhuvan.R
\name{bhuvan_stats}
\alias{bhuvan_stats}
\title{Get pre-aggregated Bhuvan NTL state statistics}
\usage{
bhuvan_stats(years = NULL, state = NULL)
}
\arguments{
\item{years}{Integer vector of years to return. \code{NULL} (default) returns
all available years (currently 2012-2024).}

\item{state}{Character. State name as returned by the API (e.g., \code{"BIHAR"}).
\code{NULL} (default) returns all states.}
}
\value{
A tidy data frame with columns \code{state} (character), \code{year}
(integer), and \code{mean_radiance} (numeric). State names are normalised to
title case (e.g., \code{"Tamil Nadu"} not \code{"TAMILNADU"}).
}
\description{
Wraps the ISRO Bhuvan NTL REST API to return mean radiance per Indian
state across years.
}
\details{
For district-level or custom-geometry statistics, use \code{\link[=bhuvan_raster]{bhuvan_raster()}}
together with \code{\link[=extract_panel]{extract_panel()}}.
}
\section{Collection break}{

Bhuvan NTL data from 2024 onwards is derived from VNP46A4 \strong{Collection
2.0}, while 2012-2023 uses the legacy \strong{Collection 1.0}. The two
collections use different calibration and atmospheric correction
algorithms, so radiance estimates are not directly comparable across this
boundary. Requests spanning both collections trigger a warning.
}

\examples{
\dontrun{
# All states, all years
df <- bhuvan_stats()

# Bihar only
df <- bhuvan_stats(state = "BIHAR")

# Multiple years
df <- bhuvan_stats(years = 2015:2023)
}
}
