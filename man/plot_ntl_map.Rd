% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_ntl_map}
\alias{plot_ntl_map}
\title{Plot a nighttime lights raster map}
\usage{
plot_ntl_map(
  raster,
  polygons = NULL,
  title = "Nighttime lights",
  caption = NULL,
  palette = c("glow", "bhuvan"),
  dark = FALSE,
  limits = NULL
)
}
\arguments{
\item{raster}{A single-layer \code{SpatRaster}. Pass one element from the list
returned by \code{\link[=ntl_download]{ntl_download()}} or \code{\link[=bhuvan_raster]{bhuvan_raster()}}.}

\item{polygons}{Optional \code{sf} object for boundary overlay.}

\item{title}{Plot title. Default \code{"Nighttime lights"}.}

\item{caption}{Optional caption string.}

\item{palette}{One of \code{"glow"} (default) or \code{"bhuvan"}.}

\item{dark}{Logical. If \code{TRUE}, the plot background is \verb{#080c14} (deep navy)
and text is light. Ignored when \code{palette = "bhuvan"} (always dark bg).
Default \code{FALSE}.}

\item{limits}{Numeric vector of length 2 for the continuous scale. Only used
when \code{palette = "glow"}. \code{NULL} uses the raster min/max. Set
explicitly when comparing multiple panels on the same scale.}
}
\value{
A \code{ggplot2} object.
}
\description{
Renders a single \code{SpatRaster} as a map. Two palettes are available:
}
\details{
\itemize{
\item \code{"glow"} (default): continuous dark-olive-to-cream-white ramp modelled on
the NASA Black Marble composite. Suited to dark backgrounds and multi-panel
year comparisons.
\item \code{"bhuvan"}: 4-class categorical legend matching the Bhuvan portal: black
(< 5 nW/cm2/sr), dark blue-grey (5-25), cream (26-200), red (> 200).
}
}
\examples{
\dontrun{
rasters <- bhuvan_raster("IND", years = 2022, bbox_pad = c(0, 0, 0, 3.5))
plot_ntl_map(rasters[["2022"]], dark = TRUE)
plot_ntl_map(rasters[["2022"]], palette = "bhuvan")
}
}
