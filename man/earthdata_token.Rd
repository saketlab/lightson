% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{earthdata_token}
\alias{earthdata_token}
\title{Get NASA Earthdata bearer token}
\usage{
earthdata_token(token = NULL)
}
\arguments{
\item{token}{Character string. If \code{NULL}, checks \code{EARTHDATA_TOKEN} env var,
then tries \code{EARTHDATA_USERNAME} + \code{EARTHDATA_PASSWORD}.}
}
\value{
The access token string, invisibly.
}
\description{
Returns the bearer token used for VIIRS/Black Marble downloads from NASA
LAADS DAAC.
}
\details{
\strong{Getting a token (web):}
\enumerate{
\item Create an account at \url{https://urs.earthdata.nasa.gov}
\item Accept the MERIS and Sentinel EULAs under Profile > EULAs
\item Go to Profile > \strong{Generate Token}, click "Generate Token", then "Show Token"
\item Copy the token (starts with \verb{EDL-}) and store it
}

\strong{Supplying the token:}
\itemize{
\item Set \code{EARTHDATA_TOKEN} environment variable (recommended: add to \verb{~/.Renviron})
\item Or pass it directly as the \code{token} argument
\item Or set \code{EARTHDATA_USERNAME} + \code{EARTHDATA_PASSWORD} to fetch it automatically
}

Bhuvan NTL functions (\code{\link[=bhuvan_stats]{bhuvan_stats()}}, \code{\link[=bhuvan_raster]{bhuvan_raster()}}) do not require
any authentication.
}
\examples{
\dontrun{
# Option 1: token from Profile > Generate Token on urs.earthdata.nasa.gov
Sys.setenv(EARTHDATA_TOKEN = "EDL-xxxxxxxxxxxxxxxxxxxx")
token <- earthdata_token()

# Option 2: username + password (fetches token automatically)
Sys.setenv(EARTHDATA_USERNAME = "myuser", EARTHDATA_PASSWORD = "mypass")
token <- earthdata_token()

# Option 3: pass directly
token <- earthdata_token("EDL-xxxxxxxxxxxxxxxxxxxx")
}
}
